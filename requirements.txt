# Stray Dog Monitoring System — dependencies

# Detection / inference (YOLO26)
# NOTE: on Windows a plain `pip install torch` is CPU-only. For GPU speed,
# install matched CUDA builds together, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# See the "GPU acceleration" section in README.md.
ultralytics>=8.3.0
torch>=2.0.0
torchvision>=0.15.0

# Computer vision
opencv-python>=4.8.0
numpy>=1.24.0

# Config
pyyaml>=6.0

# Desktop GUI
Pillow>=10.0.0

# Web app (streamlit run app2.py)
streamlit>=1.36.0
